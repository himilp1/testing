import { StyleSheet } from "react-native";

export default StyleSheet.create({
    plantPicsContainer: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'space-between',
        padding: 10,
      },      
    plantPic:{
        borderRadius: 10,
        borderColor: 'blue',
        borderWidth: 10,
        backgroundColor: 'red',
    }
})